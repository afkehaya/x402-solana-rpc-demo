# Environment configuration for x402 Solana demo
# Solana RPC endpoint for the facilitator server (fallbacks to devnet if unset)
RPC_ENDPOINT=https://api.devnet.solana.com
# Port for the facilitator HTTP server
PORT=3000
# Network for Solana (devnet or custom RPC URL)
SOLANA_NETWORK=devnet
# Skip airdrop in setup script (set to 1 to skip)
SKIP_AIRDROP=0
# Path to payer private key JSON (set by setup:demo)
# PAYER_KEYPAIR_PATH=./payer-keypair.json
# URL of the facilitator server for examples
# PROXY_URL=http://localhost:3000